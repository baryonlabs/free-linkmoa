# LinkFlow MCP Server Configuration

# JWT secret (must match web app JWT_SECRET)
JWT_SECRET=your-secret-key-change-in-production

# LinkFlow authentication token (user's JWT token)
LINKFLOW_TOKEN=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Path to LinkFlow database
# Defaults to ../../apps/web/data/linkflow.db if not specified
DATABASE_PATH=/path/to/linkflow.db

# Node environment
NODE_ENV=production
